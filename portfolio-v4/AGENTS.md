# Repository rules

## Writing style

Do not use em dashes in source code, content, documentation, or UI copy. Use commas, colons, parentheses, slashes, or regular hyphens where appropriate.

Run `pnpm check:style` after copy changes. The same rule is enforced by `pnpm check` and `pnpm build`.
