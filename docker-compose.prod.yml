version: "3.3"
services:
  react-app:
    container_name: portfolio-react-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
